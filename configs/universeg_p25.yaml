# UniverSeg in-context: 25% budget (100 patients) as the support pool, no training.
# 32 supports per draw is the largest size the 20-patient pool allows; kept
# constant across budgets so support size never confounds label budget.
dataset: camus
data_root: data/camus
model: universeg
out_dir: outputs/universeg_p25
train_subset: configs/subsets/train_p25.txt
match_keys: [view]
shots: 32
draws: 5
